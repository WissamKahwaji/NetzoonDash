export type PrivacyModel = {
  text: string;
  textEn: string;
  termofUse: string;
  termofUseEn: string;
};

export type PrivacyInputModel = {
  text?: string;
  textEn?: string;
  termofUse?: string;
  termofUseEn?: string;
};
